bindAddr = "0.0.0.0"
bindPort = 7070
#kcpBindPort = 7000
quicBindPort = 7070

vhostHTTPPort = 80
vhostHTTPSPort = 443

transport.maxPoolCount = 20
transport.tcpMux = true
transport.tcpMuxKeepaliveInterval = 60
transport.tcpKeepalive = 7200
transport.tls.force = false

webServer.addr = "0.0.0.0"
webServer.port = 7500
webServer.user = "wanchao"
webServer.password = "wanchao"
webServer.pprofEnable = false

log.to = "./frps.log"
log.level = "info"
log.maxDays = 3
log.disablePrintColor = false

auth.method = "wanchao"
auth.token = "wanchao11223344"

allowPorts = [
  { start = 10001, end = 50000 }
]

maxPortsPerClient = 8
udpPacketSize = 1500
natholeAnalysisDataReserveHours = 168
